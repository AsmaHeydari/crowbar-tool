module Dummy;
/*Expected result from Z3: true*/
/*Generated result by Z3: true*/
data NewSpec = Demonic | Prob(Rat);

[Spec: Ensures((((c / 20) - 0.5) > 0.4) || (((c / 20) - 0.5) < -0.4))]
[Spec: Prob(1/2)]
{
Int i = 0;
Int c = 0;
Int s = 0;
[Spec: WhileInv(0 <= i && i <= 20 && 0 <= s && s < 20 && 0 <= c && c < 20)]
while( i < 20 ){
    [Spec: Prob(1/2)]
    if( True ) { s = 0; }
    else { s = 1; }

    c = c + s;
    i = i + 1;
}

}